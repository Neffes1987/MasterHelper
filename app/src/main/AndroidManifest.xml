<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.masterhelper">

  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Platform.MaterialComponents">
    <activity android:name=".ui.app.settings.AbilityNamesList">
    </activity>
    <activity android:name=".ui.enemies.EditEnemy">
    </activity>
    <activity android:name=".ui.app.settings.MusicSettingsScreen">
    </activity>
    <activity
      android:name=".CreateNewItemDialog"
      android:theme="@style/AppTheme.About">
    </activity>
    <activity android:name=".ui.enemies.EnemiesListView">
    </activity>
    <activity android:name=".ui.scene.Scene">
    </activity>
    <activity android:name=".ui.journey.JourneyItemView">
    </activity>
    <activity
      android:name=".ui.journey.JourneysListView"
      android:label="@string/app_name"
      android:theme="@style/Theme.MaterialComponents.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>